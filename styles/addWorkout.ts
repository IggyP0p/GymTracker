import { StyleSheet } from "react-native";

export const AddWorkoutMenu = StyleSheet.create({

    options: {
        width: '90%', height: '40%',
        margin: 5,
        borderWidth: 2,
        borderColor: '#000000',
        borderRadius: 8
    },

});
